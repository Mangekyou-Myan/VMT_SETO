# Default config used on startup.
# The GUI lists every configs/*.txt file.
# startupConfigMode=remember starts with lastConfig.
# startupConfigMode=fixed always starts with config.
startupConfigMode=remember
config=configs/default.txt
lastConfig=configs/default.txt
